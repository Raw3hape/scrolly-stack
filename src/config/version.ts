/**
 * Application version — Foundation Projects
 *
 * Single source of truth for the site version number.
 * AI agents: update this file when deploying new features or fixes.
 *
 * Format: MAJOR.MINOR.PATCH (semver)
 * - MAJOR: breaking changes, major redesigns
 * - MINOR: new features, significant updates
 * - PATCH: bug fixes, tweaks
 *
 * Also sync package.json "version" field when updating.
 */
export const APP_VERSION = '0.1.0';
